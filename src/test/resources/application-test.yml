spring:
  docker:
    compose:
      enabled: false

  cache:
    type: none

tmdb:
  base-url: http://localhost:9999
  access-token: test-token
  default-language: en-US
  default-region: US

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false