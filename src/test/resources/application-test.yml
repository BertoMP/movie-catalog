spring:
  docker:
    compose:
      enabled: false

  cache:
    type: none

tmdb:
  base-url: ${wiremock.tmdb.base-url:http://localhost:9999}/3
  access-token: test-token

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
